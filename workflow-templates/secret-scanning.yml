name: Secret Scanning
on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  secret-scanning:
    name: Secret Scan
    uses: bayer-int/actions-workflows/.github/workflows/secret-scanning.yml@main
